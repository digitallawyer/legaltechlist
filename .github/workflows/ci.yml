name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      RAILS_ENV: test
      CI: true
      PGHOST: localhost
      PGUSER: postgres
      PGPASSWORD: postgres
      PUPPETEER_SKIP_DOWNLOAD: true
      OPENAI_API_KEY: ""
      DESCRIPTION_DRAFTS_USE_LLM: false
      PROPOSAL_TAXONOMY_USE_LLM: false
      TAG_SUGGESTION_USE_LLM: false
      PROPOSAL_ENRICHMENT_USE_LLM: false

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.3.10
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: yarn

      - name: Install JavaScript dependencies
        run: yarn install --frozen-lockfile

      - name: Prepare database
        run: bin/rails db:create db:schema:load

      - name: Run tests
        run: bin/rails test
